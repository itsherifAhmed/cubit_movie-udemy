class MovieArguments {
  int? movieId;
  String? posterPath;
  String? title;
  double? voteAverage;

  MovieArguments({
    required this.movieId,
    required this.posterPath,
    required this.title,
    required this.voteAverage,
  });
}
